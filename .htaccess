RewriteEngine On

# Форсуємо HTTPS + www в одному правилі (без подвійного редіректу)
RewriteCond %{HTTPS} !on [OR]
RewriteCond %{HTTP_HOST} !^www\. [NC]
RewriteRule ^(.*)$ https://www.carlux.com.ua/$1 [R=301,L]
